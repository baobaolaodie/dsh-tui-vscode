{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (UAT)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "--disable-workspace-trust"
      ],
      "outFiles": ["${workspaceFolder}/out/**/*.js"]
    }
  ]
}
